# cleanr-agent

Natural-language agent and command palette for cleanr.

This crate exposes the slash-command palette and a lightweight local agent that
turns plain-language instructions into structured cleanup actions.

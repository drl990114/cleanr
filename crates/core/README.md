# cleanr-core

Core data types and cleanup plan logic for cleanr.

This crate holds the domain models shared across the workspace: scan entries,
rule hits, non-overlapping cleanup plans, execution/restore manifests,
summaries, and plan-safety metadata.

# cleanr-config

Configuration loading and defaults for cleanr.

This crate defines the configuration schema, default values, config directory
resolution, and persistence helpers used by the rest of the workspace.

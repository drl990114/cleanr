api_version = "1"
id = "cleanr.builtin.system"
name = "Cleanr system cleanup rules"
version = "0.8.0"
description = "Reviewable cleanup candidates from known user-level system cleanup locations."
cleanr_version = ">=0.1.0"
capabilities = ["rules"]

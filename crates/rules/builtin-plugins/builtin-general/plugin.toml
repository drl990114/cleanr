api_version = "1"
id = "cleanr.builtin.general"
name = "Cleanr general cleanup rules"
version = "0.7.0"
description = "Lower-confidence general cleanup candidates that always require review."
cleanr_version = ">=0.1.0"
capabilities = ["rules"]

api_version = "1"
id = "cleanr.builtin.dev"
name = "Cleanr developer cache rules"
version = "0.5.0"
description = "High-confidence generated dependency and build caches for developer projects."
cleanr_version = ">=0.1.0"
capabilities = ["rules"]

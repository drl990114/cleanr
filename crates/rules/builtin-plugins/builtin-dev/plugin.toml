api_version = "1"
id = "cleanr.builtin.dev"
name = "Cleanr developer cache rules"
version = "0.10.0"
description = "Project-aware developer caches, generated dependencies, build artifacts, and review-required local data."
cleanr_version = ">=0.1.0"
capabilities = ["rules"]
